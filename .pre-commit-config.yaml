# See https://pre-commit.com for more information
# See https://pre-commit.com/hooks.html for more hooks
repos:
  - repo: local
    hooks:
      - id: lint-staged
        name: default lint staged
        entry: bash -lc "npx lint-staged"
        language: system
        pass_filenames: false
        stages: [pre-commit]